{
  "name": "ssms-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix ssms",
    "build": "npm run build --prefix ssms",
    "start": "npm run start --prefix ssms",
    "lint": "npm run lint --prefix ssms"
  }
}
